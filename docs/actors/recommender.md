# training_recommender external API
Provides an API to see what the machine learning recommender has decided that an employee should take as their next training. The details for using the API are in external_apis/recommender_api.md
